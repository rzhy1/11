name: Remove Old Workflow Runs
on:
  workflow_dispatch:

  schedule:
    - cron: '0 0 * * 0'

jobs:
  del_runs:
    name: Delete old runs
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write 
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2.0.3
        with:
          token: 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
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 3
