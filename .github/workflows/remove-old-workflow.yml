name: Remove Old Workflow Runs
on:
  workflow_dispatch:

  schedule:
    - cron: '0 0 * * 0'

jobs:
  del_runs:
    name: Delete old runs
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write 
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2.0.3
        with:
          token: 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
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 3
